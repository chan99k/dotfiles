export PATH=$HOME/development/flutter/bin:$PATH

# Obsidian vault path
export VAULT="$HOME/chan99k-workspace/chan99k's vault"

# ZenNotes CLI 기본 볼트 (zn 이 --vault 없이 볼 곳)
export ZENNOTES_VAULT="$HOME/vault"
